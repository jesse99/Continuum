# Gendarme rule ignore files.
Language: ignore
Globs: *.ignore
IgnoreWhitespace: true

Comment: \x23 [^\r\n]* [\r\n]		# note that $ won't work quite right with windows line endings
GendarmeRule: ^R:.+[\r\n]
