<!-- See http://www.opengroup.org/onlinepubs/009695399/utilities/make.html and
http://www.gnu.org/software/make/manual/html_node/index.html. Note that these
don't define make files very precisely so it's probable that this file isn't
quite right. -->

<Language name = "makefile">
	<comment>\x23 [^\r\n]* [\r\n]</comment>	<!-- note that $ won't work quite right with windows line endings -->
	<keyword>\b(?: (?&lt;! -)define | ended | ifdef | ifndef | ifeq | ifneq | else | endif | include | sinclude | override | export | unexport | VPATH | vpath)\b</keyword>
	<keyword>\b(?: DEFAULT | IGNORE | POSIX | PRECIOUS | SCCS_GET | SILENT | SUFFIXES)\b</keyword>	<!-- these should be prepended with \., but that doesn't seem to work -->
	<keyword>\$\^ | \$@ | \$% | \$\? | \$&lt; | \$\* | \$@D | \$%D | \$\?D | \$&lt;D | \$\*D | \$@F | \$%F | \$\?F | \$&lt;F | \$\*F</keyword>
	<number>(?&lt;= \W) \d+ (?= \W)</number>
	<other1>^[-\w\.\(\)/\\$]+ \s* : (?! =)</other1>		<!-- target -->
	<string>&quot; (?: [^&quot;\r\n\\] | \\. | \\\r | \\\n)* &quot;</string>
	<string>` (?: [^`\r\n\\] | \\. | \\\r | \\\n)* `</string>
	<string>&apos; (?: [^&apos;\r\n\\] | \\. | \\\r | \\\n)* &apos;</string>
</Language>


